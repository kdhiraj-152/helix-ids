# Core runtime deps for HELIX-IDS.
# Install via: pip-compile --allow-unsafe --generate-hashes -o requirements-lock.txt requirements.in
# Dev/training/deployment extras are declared in pyproject.toml [project.optional-dependencies].

torch>=2.0.0
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
pyyaml>=6.0
jsonschema>=4.0.0
tqdm>=4.62.0
