#!/usr/bin/env python3
"""Benchmark pipeline — placeholder for CI performance regression gates."""
import sys

# Placeholder: runs minimal benchmark to validate CI pipeline
print("benchmark_pipeline: placeholder OK (no real benchmarks loaded)")
sys.exit(0)
