# Dead Code Audit — Phase 19 Architecture Freeze

Generated by automated analysis and manual review.

---

## Scope

Audited across:
- `src/helix_ids/` (68 files, 27,753 LOC)
- `scripts/training/` (88 files, 27,862 LOC)
- `tests/` (100 files, 33,966 LOC)

---

## Classification

| Label | Meaning |
|---|---|
| **SAFE TO DELETE** | No callers, no tests, no references anywhere in codebase |
| **REVIEW REQUIRED** | Suspicious — may have indirect callers or be part of conditional code path |
| **KEEP** | Active / used during runtime / referenced in tests or CI |

---

## 1. Files — SAFE TO DELETE

| File | LOC | Reason |
|---|---|---|
| `scripts/training/_rewrite_main.py` | 162 | Phase 13A-4 extraction artifact — one-time migration script, never called again |
| `scripts/training/trainaugmentation.py` | (removed) | Already deleted in prior phases |
| `scripts/training/train_multidataset_v2.py` | (removed) | Already deleted in prior phases |

**Recommendation:** Delete `_rewrite_main.py`.

---

## 2. Orphan Delegate Objects — REVIEW REQUIRED

Some extracted delegate classes are instantiated but their wrappers may not be
called from the trainer. Verify:

- `_batch_processor.*()` — Check if `batch_processor` property is used
- `_warmup_manager.*()` — Check if `warmup_manager` property is used
- `_recovery_manager.*()` — Check if `recovery_manager` property is used
- `_epoch_runner.*()` — Check if `epoch_runner` property is used
- `_training_orchestrator.*()` — Check if `training_orchestrator` property is used

These were all created in Phase 13B as facade entries but may not have been
fully wired into call paths yet.

---

## 3. Wrappers — SAFE TO DELETE (30 Full Delegation)

All 30 full-delegation wrappers in HelixFullTrainer are pure pass-throughs to
delegate objects. They exist only for API compatibility with existing callers.

**Detailed inventory in [`TRAINER_FINAL_AUDIT.md`](TRAINER_FINAL_AUDIT.md#4-delegation-analysis).**

**Recommendation:** Mark with `# REMOVAL CANDIDATE — Phase 20+` docstring.

---

## 4. Duplicate Implementations — REVIEW REQUIRED

| Item | Location | Also Appears In | Assessment |
|---|---|---|---|
| `ENGINEERED_FEATURE_NAMES` | `train_helix_ids_full.py` | `src/helix_ids/data/feature_harmonization.py` | Duplicate. src version is canonical. |
| `MultiTaskNumpyDataset` | `train_helix_ids_full.py` | — | Defined in trainer file but used as module-level helper |
| `ClassBalancedIndexSampler` | `train_helix_ids_full.py` | — | Same — training helper defined at module level |
| `FrozenIndexSampler` | `train_helix_ids_full.py` | — | Same |
| `setup_logging` | `train_helix_ids_full.py` | `orchestration/run_orchestrator.py` (variant) | Duplicate; should be unified |

---

## 5. Stale Compatibility Layers — KEEP

| Item | Location | Purpose |
|---|---|---|
| `src/helix_ids/models/full.py` | `src/helix_ids/models/` | Alias module for `helix_ids_full.py` — provides stable import path |
| `src/helix_ids/contracts/immutable_constants.py` | `src/helix_ids/contracts/` | Exists for stable import; re-exports from schema_contract |

These are intentionally preserved for import-path stability.

---

## 6. Analysis: Unreachable Branches

No unreachable branches were identified by static analysis. The codebase uses
`if-elif-else` chains in:

- `_active_balance_strategy` (branch per dataset)
- `_set_epoch_loss_strategy` (branch per strategy enum)
- `_apply_loss_regularizations` (branch per regularization type)

All branches correspond to defined configuration options and are exercised
by tests or runtime configuration.

---

## 7. Unused Methods — SAFE TO DELETE

| Method | Location | LOC | Reason |
|---|---|---|---|
| `_as_python_int` | HelixFullTrainer | 5 | Converts to int; use `int()` directly |

---

## 8. Summary

| Classification | Count | LOC Impact |
|---|---|---|
| SAFE TO DELETE | 1 file + 1 method | ~167 LOC |
| REVIEW REQUIRED | 5 delegates + 1 duplicate | ~600 LOC (estimated) |
| KEEP | 2 compatibility modules + all active code | — |

**Net removability:** ~167 LOC immediately removable. Up to ~800 LOC
conditionally removable after verification.

---

## 9. Action Items

1. **Delete** `scripts/training/_rewrite_main.py`
2. **Inline** `HelixFullTrainer._as_python_int` → use `int()` directly
3. **Remove** duplicate `ENGINEERED_FEATURE_NAMES` from `train_helix_ids_full.py`
4. **Audit** 5 facade delegate properties for actual usage in call paths
5. **Extract** `MultiTaskNumpyDataset`, `ClassBalancedIndexSampler`, `FrozenIndexSampler` to `data/` subpackage
6. **Unify** `setup_logging` between trainer and orchestration
